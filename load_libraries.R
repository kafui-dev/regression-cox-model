#install.packages("survival")
library("survival")

#install.packages("tidyverse")
library(tidyverse) # pour le codestyle en pipeline

#install.packages("ggplot2")
library(ggplot2) 

#install.packages("survminer")
library(survminer) # pour la fonction survplot()

#install.packages("parameters")
library(parameters) # pour un meilleur affichage des résultats du modèle de Cox

#install.packages("GGally")
#install.packages("broom.helpers) # nécessaire pour l'utilisation de ggcoef_model
library(GGally) # pour l'affichage des résultats du modèle de Cox
